import { LightningElement } from 'lwc';

    export default class MyComponent extends LightningElement {
        // Example method for button click handling
        handleClick() {
            alert('HI, user welcome to lightning web component!');
        }
    }